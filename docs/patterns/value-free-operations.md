# Pattern: value-free operations

Coordination systems should transport references, states, and decisions—not credential values or copied production payloads. Chat Room rejects common token and private-key shapes before persistence and encourages evidence pointers instead of raw secret-bearing output.

The filter is deliberately conservative and incomplete. It supplements, but does not replace, repository-owned credential procedures and least-privilege access.
