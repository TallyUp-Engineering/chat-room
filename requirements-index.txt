# Optional. Chat Room runs without any of this — the transcript index is an
# accelerator, not a requirement, and every entry point degrades to direct file
# reads when these are absent.
#
#   chat-room index      backfill local CLI transcripts into the index
#   chat-room search      answers from the index when it exists
#
# SQLite is the default store and needs nothing further. Point
# CHAT_ROOM_DATABASE_URL at postgresql+psycopg://... to use Postgres instead.
sqlalchemy==2.0.49
alembic==1.18.4
psycopg[binary]==3.2.10
